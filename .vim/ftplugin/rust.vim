" ftplugin/rust
scriptencoding utf-8


"if !exists('g:rustc_path')
"    let g:rustc_path = z#constants#globals#Rustc_path()
"endif
"
"if !exists('g:racer_cmd')
"    let g:racer_cmd = exepath('racer')
"    if !executable(g:racer_cmd) | unlet g:racer_cmd | endif
"endif
