#!/usr/bin/env zsh

if [[ "${OSX:-0}" == "${TRUE:-1}" ]]; then
  command -p stat -c '%A %a %n' "$@"
elif [[ "${LINUX:-0}" == "${TRUE:-1}" ]]; then
  command -p stat -f '%A %a %n' "$@"
else
  exit 420
fi
